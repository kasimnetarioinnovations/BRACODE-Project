"use client";

import App from "../src/App";

/**
 * Root route for the inventory management application.
 * Renders the App component, which handles all client-side logic and UI.
 * Located in `app/page.tsx` as per Next.js App Router conventions.
 */
export default function HomePage() {
  return <App />;
}
